try {
  var dbmgr = require('dbmgr');
} catch (err) {
  var dbmgr = require('../');
}

var timeout = 1000;

var server = dbmgr.create(options);

console.log( server);

var User = function User(name){
	this.name = name;
};

var user1 = new User('yph');
var user2 = new User('yph2');

server.register(user1,function(resp){
	console.log('app.js call back result code=' + resp.code + ' body ' + resp.body);
});

server.register(user2,function(resp){
	console.log('app.js2 call back result code=' + resp.code + ' body ' + resp.body);
});

